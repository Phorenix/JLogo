package it.unicam.cs.pa.jlogo.api.commands;

/**
 * Interface representing a generic command of the Logo Language
 *
 * Each class (command) implementing this interface needs to do the proper controls on the arguments passed
 */
public interface Command {

}
